# Generated by the pipeline — not source.
# upload_manifest.json holds per-attraction Storage download tokens; those are
# the read credential for the images, so keep them out of git.
upload_manifest.json
published_list.txt
*.log
reports/
