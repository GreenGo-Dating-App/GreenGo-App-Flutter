rules_version = '2';

// ============================================================================
// FULLY OPEN RULES FOR LOCAL DEVELOPMENT TESTING
// ============================================================================
// WARNING: These rules allow ANYONE to read/write ANY file
// Use ONLY for local emulator testing
// ============================================================================

service firebase.storage {
  match /b/{bucket}/o {
    // Allow everything for testing
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
